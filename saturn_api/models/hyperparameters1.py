# coding: utf-8

"""
Saturn Cloud

The API for Saturn Cloud.

The version of the OpenAPI document: 2026.02.01
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set, Union

from pydantic import BaseModel, ConfigDict, Field
from typing_extensions import Annotated, Self


class Hyperparameters1(BaseModel):
    """
    Hyperparameters1
    """  # noqa: E501

    learning_rate: Union[
        Annotated[float, Field(le=1.0, strict=True, ge=0.0)],
        Annotated[int, Field(le=1, strict=True, ge=0)],
    ] = Field(description="Learning rate. Must be in (0, 1).")
    epochs: Annotated[int, Field(le=10, strict=True, ge=1)] = Field(
        description="Number of training epochs. Must be in [1, 10]."
    )
    effective_batch_size: Annotated[int, Field(le=128, strict=True, ge=1)] = Field(
        description="Effective batch size — the number of samples whose gradients are averaged per optimizer step. The platform translates this to axolotl's ``gradient_accumulation_steps × micro_batch_size``; ``micro_batch_size`` is auto-found at runtime to fit GPU memory."
    )
    max_seq_length: Annotated[int, Field(le=16384, strict=True, ge=128)] = Field(
        description="Maximum sequence length (tokens) per training sample. Longer sequences use proportionally more GPU memory. Must be in [128, 16384]."
    )
    lora_rank: Annotated[int, Field(le=128, strict=True, ge=1)] = Field(
        description="LoRA adapter rank. Must be in [1, 128]."
    )
    lora_alpha: Annotated[int, Field(le=256, strict=True, ge=1)] = Field(
        description="LoRA alpha scaling factor. Must be in [1, 256]."
    )
    __properties: ClassVar[List[str]] = [
        "learning_rate",
        "epochs",
        "effective_batch_size",
        "max_seq_length",
        "lora_rank",
        "lora_alpha",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Hyperparameters1 from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Hyperparameters1 from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "learning_rate": obj.get("learning_rate"),
                "epochs": obj.get("epochs"),
                "effective_batch_size": obj.get("effective_batch_size"),
                "max_seq_length": obj.get("max_seq_length"),
                "lora_rank": obj.get("lora_rank"),
                "lora_alpha": obj.get("lora_alpha"),
            }
        )
        return _obj
