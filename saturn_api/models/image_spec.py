# coding: utf-8

"""
    Saturn Cloud

    The API for Saturn Cloud.

    The version of the OpenAPI document: 2025.06.01-36
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from saturn_api.models.image_tag_recipe import ImageTagRecipe
from saturn_api.models.owner_reference import OwnerReference
from typing import Literal, Optional, Set
from typing_extensions import Self

class ImageSpec(BaseModel):
    """
    ImageSpec
    """ # noqa: E501
    name: StrictStr
    owner: Optional[OwnerReference] = None
    description: Optional[StrictStr] = ''
    versions: Optional[List[ImageTagRecipe]] = None
    hardware_type: Literal['CPU', 'NVIDIA', 'AMD'] = 'CPU'
    supports: Optional[List[StrictStr]] = None
    access: Literal['account', 'org', 'owner'] | None = None
    __properties: ClassVar[List[str]] = ["name", "owner", "description", "versions", "hardware_type", "supports", "access"]

    @field_validator('hardware_type')
    def hardware_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['CPU', 'NVIDIA', 'AMD']):
            raise ValueError("must be one of enum values ('CPU', 'NVIDIA', 'AMD')")
        return value

    @field_validator('access')
    def access_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['account', 'org', 'owner']):
            raise ValueError("must be one of enum values ('account', 'org', 'owner')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ImageSpec from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of owner
        if self.owner:
            _dict['owner'] = self.owner.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in versions (list)
        _items = []
        if self.versions:
            for _item_versions in self.versions:
                if _item_versions:
                    _items.append(_item_versions.to_dict())
            _dict['versions'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ImageSpec from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "owner": OwnerReference.from_dict(obj["owner"]) if obj.get("owner") is not None else None,
            "description": obj.get("description") if obj.get("description") is not None else '',
            "versions": [ImageTagRecipe.from_dict(_item) for _item in obj["versions"]] if obj.get("versions") is not None else None,
            "hardware_type": obj.get("hardware_type") if obj.get("hardware_type") is not None else 'CPU',
            "supports": obj.get("supports"),
            "access": obj.get("access")
        })
        return _obj


