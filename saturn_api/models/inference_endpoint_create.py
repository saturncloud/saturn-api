# coding: utf-8

"""
Saturn Cloud

The API for Saturn Cloud.

The version of the OpenAPI document: 2026.02.01
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Literal, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Annotated, Self


class InferenceEndpointCreate(BaseModel):
    """
    InferenceEndpointCreate
    """  # noqa: E501

    name: Annotated[str, Field(min_length=1, strict=True)] = Field(
        description="Human-readable name for the inference endpoint."
    )
    checkpoint_artifact_id: Annotated[str, Field(min_length=1, strict=True)] = Field(
        description="Artifact id of the checkpoint to serve. Must be a ``kind=checkpoint`` Artifact with ``status=ready`` belonging to the same org as the requester."
    )
    instance_size: Annotated[str, Field(min_length=1, strict=True)] = Field(
        description="Saturn instance size to run the inference pod on. Must be a GPU-equipped size (gpu > 0)."
    )
    quantization: Literal["fp8", "int8"] | None = None
    visibility: Literal["org", "owner"] = "org"
    viewers: Optional[List[Annotated[str, Field(min_length=1, strict=True)]]] = Field(
        default=None,
        description="Optional list of identity names (usernames or group names in the endpoint's org) granted access to the endpoint route in addition to the owner. Honored by ForwardAuth exactly like a normal deployment's viewers.",
    )
    __properties: ClassVar[List[str]] = [
        "name",
        "checkpoint_artifact_id",
        "instance_size",
        "quantization",
        "visibility",
        "viewers",
    ]

    @field_validator("quantization")
    def quantization_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["fp8", "int8"]):
            raise ValueError("must be one of enum values ('fp8', 'int8')")
        return value

    @field_validator("visibility")
    def visibility_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["org", "owner"]):
            raise ValueError("must be one of enum values ('org', 'owner')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InferenceEndpointCreate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if quantization (nullable) is None
        # and model_fields_set contains the field
        if self.quantization is None and "quantization" in self.model_fields_set:
            _dict["quantization"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InferenceEndpointCreate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "name": obj.get("name"),
                "checkpoint_artifact_id": obj.get("checkpoint_artifact_id"),
                "instance_size": obj.get("instance_size"),
                "quantization": obj.get("quantization"),
                "visibility": obj.get("visibility") if obj.get("visibility") is not None else "org",
                "viewers": obj.get("viewers"),
            }
        )
        return _obj
