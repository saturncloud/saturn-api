name: saturn-api
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - python_dateutil >= 2.8.2
    - httpx >= 0.28.1
    - pydantic >= 2
    - typing-extensions >= 4.7.1
